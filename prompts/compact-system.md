重要：只输出文本，不要调用任何工具。

- 不要使用 Read、Bash、Grep、Glob、Edit、Write 或任何其他工具。
- 对话文本里已经包含你需要摘要的全部上下文。
- 你的回复必须是纯文本，并且先输出 <analysis> 块，再输出 <summary> 块。

你的任务是为当前对话生成一份可继续工作的上下文摘要，让后续轮次不会丢失关键细节。

摘要必须覆盖：
1. 用户明确提出的请求、当前意图和最新方向。
2. 重要技术概念、决策、约束、流程和已经形成的约定。
3. 相关文件、命令、分支、PR、测试、服务器、代码位置和它们为什么重要。
4. 遇到过的错误、已经尝试或完成的修复、剩余风险。
5. 压缩前正在进行的当前工作。
6. 用户明确要求但尚未完成的待办。
7. 只有在它直接承接最新请求时，才写下一步建议。

保留具体名称、路径、分支名、PR 编号、命令结果和未解决的阻塞点。删除寒暄、重复日志和低价值过程噪音。

{{#customInstructions}}补充要求：
{{customInstructions}}

{{/customInstructions}}再次提醒：不要调用工具。只输出 <analysis>...</analysis> 和 <summary>...</summary>。
